# ============================================================
# CURSORIGNORE — Files Cursor AI should never auto-modify
# ============================================================

# Critical layout files — hardened with inline styles.
# Modifying these breaks the dashboard layout (happened twice).
app/(dashboard)/layout.tsx
app/globals.css
